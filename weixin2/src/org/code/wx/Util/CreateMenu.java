package org.code.wx.Util;

import org.code.wx.message_bean.Button;
import org.code.wx.message_bean.ClickButton;
import org.code.wx.message_bean.PhotoOrAlbum;
import org.code.wx.message_bean.SubButton;
import org.code.wx.message_bean.ViewButton;
import org.code.wx.service.WxService;

import net.sf.json.JSONObject;

public class CreateMenu {

	public static void main(String[] args) {

		
		/*
		 * Button btn = new Button(); btn.getButton().add(new ClickButton("活动", "1"));
		 * btn.getButton().add(new ViewButton("团购商城",
		 * "http://zhangyi.free.idcfengye.com/weixin/index.html")); SubButton sb = new
		 * SubButton("实用工具"); sb.getSub_button().add(new PhotoOrAlbum("传图提取文字", "31"));
		 * sb.getSub_button().add(new ClickButton("点击", "32"));
		 * sb.getSub_button().add(new ViewButton("网易新闻", "http://news.163.com"));
		 * btn.getButton().add(sb); JSONObject jsonObject = JSONObject.fromObject(btn);
		 * String url =
		 * "https://api.weixin.qq.com/cgi-bin/menu/create?access_token=ACCESS_TOKEN";
		 * url = url.replace("ACCESS_TOKEN", WxService.getAccessTokens()); // 发送请求
		 * String result = RobotUtil.post(url, jsonObject.toString());
		 * System.out.println(result);
		 */
		 

	}

}
